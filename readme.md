# Planeta GAS: Система бронирования

Современная система управления бассейном на базе Google Sheets с JSON-ориентированной архитектурой.

## 🏗 Архитектура
Проект переведен на модульную структуру с четким разделением слоев:
- **Repository Layer**: Инкапсуляция всей работы с Google Таблицами. Использует маппинг Row ↔ JSON.
- **Service Layer**: Бизнес-логика (бронирование, логика продаж, проверка конфликтов).
- **UI Layer**: Динамический Sidebar на Vanilla JS + CSS с табовой навигацией.

## 🗂 Файловая структура
- `BaseRepository.gs`: Базовый класс для работы с листами.
- `*Repository.gs`: Репозитории для сущностей (Клиенты, Расписание, Продажи и т.д.).
- `*Service.gs`: Сервисы с бизнес-логикой.
- `constants.gs`: Централизованные настройки, имена листов и контракты данных (Column Mappings).
- `ui.gs`: Входные точки для UI и бэкенд-обработчики.
- `sidebar.html`: Единый интерфейс приложения.
- `schemaValidator.gs`: Автоматическая проверка структуры таблиц и UUID.

## 🚀 Основные возможности
- **Умное расписание**: Авто-проверка конфликтов тренера и клиента в реальном времени.
- **История клиента**: Мгновенный просмотр последних покупок и занятий в карточке клиента.
- **Задачи**: Табличный вид задач с прямыми ссылками на ячейки в листах.
- **Валидация**: Защита от некорректного ввода (скидки, форматы дат и времени).

## 🛠 Разработка
- **Синхронизация**: Используется `clasp` и скрипт `sync_to_gas.sh`.
- **Тесты**: Встроенные Unit-тесты (`repositoryTests.gs`) и диагностика (`diagnosticTests.gs`).
