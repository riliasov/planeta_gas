# Текущий контекст: Planeta GAS
**Дата: 24.12.2025**

## Статус проекта: Рефакторинг завершен ✅
Проект успешно переведен на архитектуру на базе репозиториев и JSON-моделей.

## Текущее состояние
1.  **Repository Pattern**: Все листы (7 листов) работают через классы-наследники `BaseRepository`. 
    - `Schedule`: 14 колонок (A:N).
    - `Clients`: 10 колонок (A:J).
    - `Sales`: 23 колонки (A:W).
2.  **Sidebar**: Полностью обновлен под новые модели данных.
    - Исправлены поля `phone`, `price`, `childDob`.
    - Оптимизированы Typeahead-поиски.
    - Раздел "Задачи" переведен на табличный вид с прямыми ссылками.
3.  **Автоматизация**: 
    - Реализован генератор и филлер UUID для всех записей.
    - Внедрена валидация схем (`schemaValidator.gs`).
4.  **Синхронизация**: Настроен безопасный пайплайн деплоя через `sync_to_gas.sh`.

## Структура данных (Ключевые контракты)
- **Schedule**: `PK` в колонке N (13).
- **Clients**: `PK` в колонке J (9). Лист переименован в "Клиенты".
- **Sales**: `PK` в колонке W (22).

## Что дальше (Backlog)
- [ ] Оптимизация производительности при больших объемах данных (Batching).
- [ ] Добавление управления Lead-листом из Sidebar.
- [ ] Реализация полноценной админки для редактирования/удаления записей.
